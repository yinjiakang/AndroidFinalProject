<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:layout_weight="1.0" >

    <!-- <ListView
        android:id="@+id/home_lv_mark"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:background="@color/backgroundcolor"
        android:cacheColorHint="@color/backgroundcolor"
        android:divider="@drawable/list_divider"
        android:fadingEdge="none"
        android:listSelector="@drawable/list_selector" />

     <ProgressBar
        android:id="@+id/home_progress_like"
        style="?android:attr/progressBarStyleInverse"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:indeterminateDrawable="@drawable/progressbar"
        android:visibility="visible" /> -->
       
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical" >

        <EditText
            android:id="@+id/markactivity_title"
            android:layout_marginTop="5dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="标题" />

        <EditText
            android:id="@+id/markactivity_content"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="top"
            android:hint="歌曲名称、作者或者歌词"
            android:inputType="textMultiLine"
            android:minLines="12" />

        <Button
            android:id="@+id/markactivity_post"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="我要听" />
>
    </LinearLayout>

</FrameLayout>